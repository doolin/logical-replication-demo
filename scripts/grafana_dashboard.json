{
  "dashboard": {
    "id": null,
    "uid": null,
    "title": "Testem Dashboard",
    "tags": ["templated"],
    "timezone": "browser",
    "schemaVersion": 16,
    "version": 0,
    "refresh": "25s",
    "panels": [
      {
        "title": "New panel",
        "type": "graph",
        "id": 1,
        "targets": [
          {
            "refId": "A",
            "type": "time_series",
            "datasource": "InfluxDB",
            "targets": [
              {
                "target": "from(bucket: \"ruby_test\") |> range(start: v.timeRangeStart, stop: v.timeRangeStop) |> filter(fn: (r) => r[\"_measurement\"] == \"locks\") |> filter(fn: (r) => r[\"_field\"] == \"lock_count\") |> filter(fn: (r) => r[\"mode\"] == \"AccessShareLock\" or r[\"mode\"] == \"ExclusiveLock\" or r[\"mode\"] == \"RowExclusiveLock\" or r[\"mode\"] == \"ShareLock\" or r[\"mode\"] == \"ShareUpdateExclusiveLock\")                "
              }
            ],
            "legendFormat": "{{your_label}}",
            "interval": "1s"
          }
        ],
        "gridPos": {
          "h": 9,
          "w": 12,
          "x": 0,
          "y": 0
        }
      }
    ]
  },
  "overwrite": false
}

